...Github...

# Initialize a new Git repository :
    git init 

# Clone an existing repository :
    git clone <repository-url>

# Set global username and email (First Time or One time setup) :
    git config --global user.name "Your Name"
    git config --global user.email "your.email@example.com"

# Add one file :
    git add <filename>

# Add all files :
    git add .

# Commit staged changes with a message :
    git commit -m "Your commit message"

# Push file :
    git push origin main


#create venv (open anaconda prompt and this command using C drive storage)
conda create -n venv python=3.9 -y

#active venv
conda activate venv

#check env
conda env list

##create venv (open open vs code > cmd  and go to the exact folder and this command using project folder storage storage)
conda create --prefex .\venv python=3.9 -y

#activate venv
conda activate .\venv

#requirements.txt
pip install -r requirements.txt


